export enum Directions {
  UP = "up",
  RIGHT = "right",
  LEFT = "left",
  DOWN = "down",
  UNKNOWN = "",
}
export enum EnumDimensions {
  SCREEN_WIDTH = 600,
  SCREEN_HEIGHT = 400,
  BLOCK_SIZE = 25,
}
export enum Config {
  DELAY = 50,
  MAX_POINTS = 10,
}
export enum KeyMap {
  UP = 'ArrowUp',
  LEFT = 'ArrowLeft',
  RIGHT = 'ArrowRight',
  DOWN = 'ArrowDown',
  SPACEBAR = 'Space',
}
