export enum Config {
  BLOCK_SIZE = 25,
}
export enum Directions {
  UP = "up",
  RIGHT = "right",
  LEFT = "left",
  DOWN = "down",
}
export enum EnumDimensions {
  SCREEN_WIDTH = 600,
  SCREEN_HEIGHT = 400,
  BALL_RADIUS = 15,
  BRICK_WIDTH = 81,
  BRICK_HEIGHT = 9,
}
