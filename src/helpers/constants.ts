export enum Directions {
  UP = "up",
  RIGHT = "right",
  LEFT = "left",
  DOWN = "down",
  UNKNOWN = "",
}
export enum EnumDimensions {
  SCREEN_WIDTH = 600,
  SCREEN_HEIGHT = 400,
  BLOCK_SIZE = 25,
}
export enum Config {
  DELAY = 50,
  MAX_POINTS = 10,
  BONUS_TIME = 5000,
  MESSAGE_TIME = 3000,
}
export enum KeyMap {
  UP = "ArrowUp",
  LEFT = "ArrowLeft",
  RIGHT = "ArrowRight",
  DOWN = "ArrowDown",
  SPACEBAR = "Space",
}
export enum EnumMessages {
  NEXT_LEVEL = "Next Level",
  GAME_OVER = "Game Over",
}
